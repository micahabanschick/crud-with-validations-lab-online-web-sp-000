<<<<<<< HEAD
<!-- <% @songs.each do |song| %>
  <h1><%= song.title %></h1>
  <p><%= song.release_year %></p>
  <p><%= song.genre %></p>
  <p><%= song.artist_name %></p>

  <%= link_to 'Show', song_path(song) %> |
  <%= link_to 'Edit', edit_song_path(song) %> |
  <%= link_to 'Delete', song_path(song), method: :delete, data: { confirm: 'For real?' } %>
<% end %> -->

<h1>Songs</h1>

<div><%= link_to 'Create Song', new_song_path %></div>
<br />

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Year</th>
      <th>Genre</th>
      <th>Artist name</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= song.title %></td>
        <td><%= song.release_year %></td>
        <td><%= song.genre %></td>
        <td><%= song.artist_name %></td>
        <td>
          <%= link_to 'View', song %> |
          <%= link_to 'Edit', edit_song_path(song) %> |
          <%= link_to 'Delete', song, method: :delete, data: { confirm: 'For real?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
=======
<% @songs.each |song| do %>
  <h1><%= song.title %></h1>
  <% song.each |atr| do %>
    <p><%= atr %></p>
  <% end %>
  <br>
<% end %>
>>>>>>> 436a11b6f76adbc01840856dd3abffe4c5dab7b3
